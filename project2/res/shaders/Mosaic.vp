#version 430 core
layout (location = 0) in vec2 aPos;
layout (location = 1) in vec2 aTexCoords;

out vec2 TexCoords;

void main()
{
    TexCoords = aTexCoords;
    // 直接輸出到螢幕的 NDC 座標 (-1 到 1)，不需要 MVP 矩陣！
    gl_Position = vec4(aPos.x, aPos.y, 0.0, 1.0);
}
